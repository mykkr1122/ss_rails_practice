# cp .env.example .env して使う。 .env は git に載せない。

# Docker の MySQL root パスワード（docker-compose と揃える）
MYSQL_ROOT_PASSWORD=password

# Rails から見た接続先
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USERNAME=root
MYSQL_PASSWORD=password
